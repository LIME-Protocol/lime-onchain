{
  "name": "lime-onchain",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "sdk"
  ],
  "scripts": {
    "build:sdk": "npm --workspace sdk run build",
    "bootstrap:devnet": "bash scripts/bootstrap-devnet.sh",
    "test:anchor": "ts-mocha -p ./tsconfig.json -t 1200000 tests/**/*.ts",
    "lint": "eslint . --ext .ts"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "chai": "^4.5.0",
    "ts-mocha": "^10.1.0",
    "typescript": "^5.9.2"
  }
}
